# アーキテクチャ

このプログラムが何をどう分けているか、そしてなぜそう分けたかを説明します。個々の判断の理由はコード中のコメントにあります。ここに書くのは、そのコメントを読みに行く前に知っておくと迷わずに済むことだけです。

## 何をするものか

Baballonia 互換の口トラッキングカメラを PaperTracker クライアントへ橋渡しします。カメラから取ったフレームを、そのクライアントが期待する MJPEG-over-HTTP ストリームとしてループバック上に配信し直します。

フレームはこう流れます。

```
カメラ ──▶ source ──▶ (変換) ──▶ hub ──▶ server ──▶ PaperTracker クライアント
```

その経路を組み立て、実行時に差し替えるのが `bridge` です。差し替えを指示するのは
トレイメニューか管理 API で、指示された内容は `config` を通して設定ファイルへ戻ります。

```
tray ──┐
       ├──▶ bridge ──▶ 上の経路を止めて組み直す
API ───┘        │
                └──▶ config ──▶ 設定ファイル
```

## 全体の形

**副作用を持たない核 (`core`) を、命令的な外殻が取り囲む**という分け方です。

`core` は JPEG の検証、ワイヤプロトコルの解析、multipart のエンコード、画像変換を持ちます。このパッケージはソケット・シリアルポート・子プロセス・ファイルシステムのいずれにも触れません。**この制約があるから、カメラを 1 台も繋がずにプロトコル処理をテストできます。** ETVR のパケット形式も、Content-Length を省くファームウェアも、EXIF のサムネイルに紛れた偽の EOI も、すべてバイト列を渡すだけで確かめられます。

外殻は、実際に世界と話す部分です。`source` がカメラから読み、`server` がクライアントへ書き、`config` がファイルを読み書きし、`tray` が画面を出します。これらは `core` を使いますが、`core` はこれらを知りません。

## パッケージ

### フレームが流れる経路

| | |
|---|---|
| **`core`** | 副作用の無いロジック。JPEG の構造走査、ETVR パケットの解析、multipart のエンコード、幾何変換。他の内部パッケージに依存しません |
| **`source`** | カメラから読むドライバ群。ffmpeg の子プロセス越しの UVC、シリアル越しの有線ボード、既存の MJPEG ストリームの中継。**どれも自力で再接続します** |
| **`hub`** | 1 生産者・多消費者のフレーム配信。**最新フレーム優先**で、遅いクライアントがソースの読み取りループを止めることはありません |
| **`server`** | ストリーム・状態・管理の HTTP エンドポイント |
| **`bridge`** | 上記を繋ぎ、その生存期間を所有します。トレイと管理 API が、ドライバの作り方を知らないまま実行時にソースを変更できるのはこのためです |

### 支える側

| | |
|---|---|
| **`config`** | 設定の読み込み・検証・保存。優先順位はコマンドライン > 環境変数 > TOML ファイル > 既定値 |
| **`status`** | 稼働中のソースの健全性。`/healthz`・`/stats`・トレイが読みます |
| **`i18n`** | 人が読むテキスト。**ログと API 応答は対象外**です（後述） |
| **`tray`** | Windows のシステムトレイ。このプログラムが持つ唯一の UI |
| **`ffmpegfetch`** | ffmpeg の取得。同梱していない理由はパッケージのコメントにあります |
| **`papertracker`** | クライアントのアドレスキャッシュの書き換えと復元 |
| **`logging`** / **`autostart`** / **`console`** | ログのローテーション、サインイン時の起動登録、GUI プロセスの端末への再接続 |

## 依存の向き

内部パッケージが直接 import している相手です (`go list` の出力そのもの)。

| | |
|---|---|
| `cmd/ptcambridge` | すべて |
| `bridge` | `config`, `core`, `hub`, `server`, `source`, `status` |
| `server` | `config`, `core`, `ffmpegfetch`, `hub`, `source`, `status` |
| `tray` | `config`, `ffmpegfetch`, `hub`, `i18n`, `status` |
| `source` | `core`, `ffmpegfetch`, `i18n` |
| `config` | `core`, `i18n` |
| `ffmpegfetch` | `config` |
| `hub` | `core` |
| `core` / `status` / `i18n` / `papertracker` / `logging` / `autostart` / `console` | 標準ライブラリのみ |

**逆流はありません。** 下の 7 つは葉で、標準ライブラリ以外に何も依存しません。特に `status` は `bridge`・`server`・`tray` の 3 つから直接読まれますが、そのどれも知りません。「どのエラーが翻訳に値するか」の判定がドライバの領分である `source.ErrorKey` に置かれ、`main` が `status.WithErrorKeys` で注入しているのはそのためです。

## 知っておくと読みやすい判断

### ソースが「動いている」とは、フレームが 1 枚届くこと

ドライバはカメラに届かなくても失敗せず再接続します。つまり**「まだエラーが出ていない」は何も証明しません**。**キャプチャに関わる設定を変更する場合**、`bridge.Apply` が新しい設定を残すのは、最初のフレームが `hub` まで到達したときだけです。

キャプチャに触れない変更 — `server.boundary`、`server.extra_headers`、`server.hold_on_source_loss` — はこの経路を通りません。カメラをそのままにして即座に採用します。再起動すれば何の得も無くストリームが途切れますし、カメラがたまたま再接続中なら検証が変更を丸ごと拒否してしまいます。まさにその状況のための設定である `hold_on_source_loss` さえも。

見るのがドライバではなく `hub` なのは、その間に変換段があるからです。ピクセル上限を超えた画像やデコーダが拒む画像はそこで落ちます。クライアントに何かが見えると言えるのは、`hub` まで届いたフレームだけです。

一方**起動時と再開時は検証しません**。まだ挿さっていないカメラは、現れたときに拾えなければならないからです。この非対称はコード全体に効いています。

### 構造は画像ではない

フレームごとの検査に許されるのは構造の確認までです。しかし SOI の直後に EOI が来るペイロードは JPEG の構造を備えていて中身は空です。それを送るソースは最後まで健全に見えます — フレーム数は増え、`/healthz` は緑、トレイには fps — その間トラッカーは使えるものを何も受け取りません。

そこで **1 枚デコードできるまで何も配信しません**。デコードは高くつくので、それは 1 回だけです。

### 設定は「動く設定」を守る

`bridge` は `proven` フラグで「その設定が実際にドライバを起動できたか」を追います。検証に失敗した変更を巻き戻す先が本当に動いていたのか、一時停止中の変更を拒む理由が実在するのか。この 2 つの判断がそれを必要とします。

**一時停止はこのフラグを下ろしません。** 意図的に止めたソースも依然として動くソースであり、それこそが守る対象だからです。

### 保存は実効設定ではなくファイルを土台にする

`-device` や `PTCAMBRIDGE_*` はその実行のためのものです。実効設定をそのまま書き戻すと、トレイが無関係な何かを変えた最初の瞬間に、一度きりのはずの上書きが恒久化します。だから `bridge` は「ファイルが述べていた内容」を別に持ち、**変更した葉だけ**をその上に重ねます。

### 翻訳するもの、しないもの

| | |
|---|---|
| **翻訳する** | トレイ UI、コンソール出力、ユーザーが対処できる 3 つのエラー（カメラ未設定 / ffmpeg 未検出 / カメラボードらしいポートが無い） |
| **翻訳しない** | ログ、`/stats` と `/api/v1/*` の応答、対処できないエラー |

ログは不具合報告に貼られ、その文言で検索されます。**実行した機械によって言語が変わるログは、後から読む全員にとって価値が下がります。** API 応答はプログラムが読むので安定している方が重要です。対処できないエラー（拒否されたポート、途中で消えたデバイス）は、ドライバが書いた詳細そのものに価値があり、翻訳した要約では情報が減ります。

### 管理 API に認証は無い

だから**ループバックに bind しているときしか提供しません**。ただしループバックであること自体は防御になりません。ユーザーが訪れたどのページも 127.0.0.1 に到達できます。`server.guardAdmin` が Origin・Host・`Sec-Fetch-Site`・Content-Type を見ているのはそのためです。

## テストの方針

- **`core` はハードウェア無しで全経路を通せます。** これがこの分け方の主な見返りです
- **ドライバは注入点を持ちます。** `source.Serial` の `listPorts` と `openPort` は差し替え可能です。候補ポートの巡回や、停滞したセッションが線について何を語るかは、列挙結果と読み取り内容を操作できなければ確認できません
- **翻訳には 2 つの不変条件をテストで強制しています。** 全キーが全言語を持つこと、書式指定子が言語間で一致すること。前者が無いと半分だけ翻訳されたメニューになり、それは英語のままより悪く「言語が無い」のではなく「バグ」に見えます。後者が無いと `%!d(string=uvc)` がユーザーの画面に出ます

## 制約

- **トレイは Windows のみ。** PaperTracker クライアントが配布されているのがそのプラットフォームだからです。それ以外ではヘッドレスで動き、同じ操作面は管理 API から使えます
- **cgo 無しの単一実行ファイル。** UVC のキャプチャを ffmpeg の子プロセスに委ねているのも、`golang.org/x/text` を使わず自前の小さなカタログにしているのも、この方針から来ています
